{
  "name": "<% appName %>",
  "version": "0.1.0",
  "author": {
    "name": "<%= authorName %>",
    "email": "<%= authorEmail %>",
  },
  "description": "<% appDescription %>",
  "scripts": {
    "watch": "babel js -d dist -w"
  },
  "devDependencies": {
    "babel-cli": "^6.8.0",
    "babel-preset-es2015": "^6.6.0"
  }
}
